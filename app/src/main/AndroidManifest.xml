<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.storesoko.entertainmentapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EntertainmentApp">
        <activity
           android:theme="@style/NoNav"
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Theme.EntertainmentApp"
            android:name=".News.Ui.MainActivity"
            android:exported="false" />
        <activity
            android:theme="@style/Theme.EntertainmentApp"
            android:name=".Age.Ui.AgeActivity"
            android:exported="false" />
        <activity
            android:theme="@style/Theme.EntertainmentApp"
            android:name=".Movies.Ui.MoviesActivity"
            android:exported="false" />
    </application>

</manifest>